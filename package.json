{
  "name": "plugin-compressorjs",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && rollup -c && cp src/index.js dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Margox",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "compressorjs": "^1.0.5"
  },
  "devDependencies": {
    "rollup": "^1.15.6",
    "rollup-plugin-commonjs": "^10.0.0",
    "rollup-plugin-node-resolve": "^5.0.3"
  }
}
